1.4.1
Bug Fixes
- Error messages now tell you exactly what went wrong, instead of just saying something is wrong.
- Pasting multiple objects at once no longer wipes out the rest of your list. Your existing work stays safe.
- The update popup no longer hides part of the message behind its buttons.
New Feature
- The app is now available in Spanish, German, French, and Simplified Chinese, next to English. Pick your language on first launch, or change it anytime in Settings.
- The title bar now matches the app's look, and dims when the window is not active.
- You can add a subtle color fade to the background, choose its direction, or turn it off in Settings.
- Buttons now resize automatically so text always fits, in every language.
------------------------------
v1.4.0
Bug Fixes
- Auto-save backups were accumulating unnecessarily and taking up disk space. These are now completely disabled.
New Feature
- When you update to this version and have a lot of old backups stored up, the app will ask you once what to do with them so nothing gets deleted without your permission.
- The app now has a menu bar at the top with easy access to Ship Slot Manager, Backups, Settings, and other options like Donate, Info, and Help.
- Settings is now a full page where you can control everything about how the app works, including what your backup folders are and how many backups to keep.
- You can now choose your own backup location instead of using the default folder. If you use multiple Steam accounts, each one gets its own folder automatically so backups don't get mixed up.
- Backups are now smarter about keeping old files. Instead of deleting by date, the app keeps only the number of backups you want and automatically removes older ones.
- Font sizes throughout the app can be customized to make everything bigger or smaller depending on what's comfortable for you.
------------------------------
v1.3.8
Bug Fixes
- Save As and .so/.nmsship import no longer allow Freighters, Bases, External Bases, or Temp Stored Corvettes to be exported/imported as .so or .nmsship - only Corvettes have a linked Ship Ownership slot, so only Corvettes can use those formats. Other types can still be saved as .json/.txt.
- Save As on Expedition Corvettes did nothing (loading screen flashed but the save dialog never opened).
------------------------------
1.3.7
Bug Fixes
- Added Torpedo Launcher to the weapon priority list.
- Fixed: closing the update popup no longer snoozes the update for 14 days.
- Fixed: pasting a single object into the JSON box now correctly appends it (at the cursor position, automatically handling a stray trailing comma from copying it out of a list) instead of replacing the whole JSON, while - still keeping BaseType, OriginalBaseVersion, and UserData protection intact - so pasting a Planet Base's code into a selected Corvette no longer turns it into a Planet Base.
New Feature
- Added a bug notice banner that shows a short message when needed.
------------------------------
v1.3.6
New Feature
- Added Custom Starship support in SSM, Now save to move Custom Starships from slot without losing the customization.
------------------------------
v1.3.5
Bug Fixes
- Fixed several issues in the Ship Slot Manager (SSM).
- Fixed an issue where the object list in Single File Mode did not update after pasting new JSON.
New Features
- Added support for loading .hg files in Single File Mode for GOG users.
- Added a confirmation dialog before saving changes in the Ship Slot Manager (SSM).
- Added automatic class names for unnamed starships (Hauler, Explorer, etc.).
- Added a Browse option in Restore Backup for save files stored in a custom location.
- Added a Credits section, including three Ambassadors: Arcane Traveller, ChefZiggy and Aresmar, who have been involved since the beginning of the project, helping with promotion, bug hunting and providing valuable UI feedback.
------------------------------
v1.3.4
Bug Fixes
- Pasting a single object into the JSON box now appends instead of replacing the existing JSON.
New Feature
- Object sizes are now displayed next to each object in the object list.
------------------------------
v1.3.3
- Added a new corvette can now be done in the main ui, there will be a button if not maxed (Add Corvette) below the corvette list of the main save and expedition.
- Several people have asked if they could support the project, so I've added this tip jar with a Paypal button.




